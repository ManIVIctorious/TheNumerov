# Executable name
  EXENAME = WatsonPrep

# Additional libraries, includes and packages
  LIB      = -lm
  INC      = 
  PACKAGES = gsl

# Preprocessor flags
# PPF += -Ddebug_normalmode
# PPF += -Ddebug_coords
# PPF += -Ddebug_moment_of_inertia
# PPF += -Ddebug_mu

# Source Files
  SRC += main.c
  SRC += Help.c
  SRC += SetDefaultSettings.c
  SRC += GetSettingsGetopt.c
  SRC += InputNormalMode.c
  SRC += InputFileList.c
  SRC += InputComFile.c
  SRC += MomentOfInertia.c
  SRC += InvertMatrix.c
